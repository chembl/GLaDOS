// Generated by CoffeeScript 1.4.0
var SideMenuHelper;

SideMenuHelper = (function() {

  function SideMenuHelper() {}

  SideMenuHelper.initializeTopMenuController = function() {
    var sidenav_fixed, win;
    win = $(window);
    sidenav_fixed = $(".side-nav.fixed");
    return win.scroll(function() {
      var maxPadding, top;
      maxPadding = 32;
      top = win.scrollTop();
      if (top > maxPadding) {
        sidenav_fixed.css({
          'paddingTop': '0px'
        });
      }
      return {
        "else": sidenav_fixed.css({
          'paddingTop': maxPadding - top + 'px'
        })
      };
    });
  };

  /* *
    *  This is necessary to fix the bug of the side nav not appearing correctly.
  */


  SideMenuHelper.complementaryinitSideNav = function() {
    return $('.button-collapse').each(function() {
      var currentBtn;
      currentBtn = $(this);
      return currentBtn.click(function() {
        var activated;
        activated = $('#' + $(this).attr('data-activates'));
        return activated.show();
      });
    });
  };

  return SideMenuHelper;

})();
